extends layouts/main-layout.pug

block content
            main
                h2 Products
                if products.length <= 0
                    p No products found.
                else
                ul
                    each product in products
                        li #{product.title}

            footer
                p © 2020 Shop